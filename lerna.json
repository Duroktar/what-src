{
  "command": {
    "publish": {
      "ignoreChanges": [
        "*.md",
        "*.txt",
        "test/**",
        "@(!(package)).json"
      ]
    }
  },
  "packages": [
    "packages/*"
  ],
  "version": "1.0.1",
  "npmClient": "yarn",
  "npmClientArgs": [
    "--no-lockfile"
  ]
}
