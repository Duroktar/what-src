{
  "name": "@what-src/utils",
  "version": "1.1.0-alpha.0",
  "description": "what-src common utilities.",
  "author": "Scott Doucet <duroktar@gmail.com>",
  "license": "MIT",
  "repository": "https://github.com/duroktar/what-src/tree/master/packages/what-src-utils",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean:dist": "rimraf ./dist",
    "build": "cross-env NODE_ENV=production tsc -p .",
    "build:watch": "tsc -p tsconfig.json --watch",
    "build:utils": "npm run build",
    "build:dist": "npm run build"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=6.9.0"
  },
  "devDependencies": {
		"@types/lodash.throttle": "^4.1.6",
    "@types/node": "^12.7.8",
		"lodash.throttle": "^4.1.1",
    "typescript": "^3.6.3"
  }
}
